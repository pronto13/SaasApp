Memberships
* User should see link for Basic plan on homepage
* User should see link for Pro plan on home page
* Basic plan link should plan to regular Devise sign-up form
* Pro plan link should point to a custom sign-up form

Stripe integration
* Users should have a stripe customer token in the users table
* Javascript should prevent Pro form from submitting, when user fills it out, 
and should card info to stripe, Stripe will return with a card token
* Javacript should send user fields and card token to our rails app for validation
Rails app should validate user fields. Rails app will note whether plan 1 or 2
* Once validated, Rails should send user info ( email, plan id and card token)
to Stripe server and Stripe will create a subscription and send back customer token
Rails should save customer token and user info to our database

